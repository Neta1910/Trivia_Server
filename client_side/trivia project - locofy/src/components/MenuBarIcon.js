import styles from "./MenuBarIcon.module.css";

const MenuBarIcon = () => {
  return (
    <img
      className={styles.menuBarIcon}
      loading="lazy"
      alt=""
      src="/menu-bar1.svg"
    />
  );
};

export default MenuBarIcon;
